{
  "name": "api",
  "module": "src/index.ts",
  "private": true,
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.8.1",
    "@types/supertest": "^6.0.3",
    "prisma": "^6.17.1",
    "supertest": "^7.2.2",
    "ts-node-dev": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bunx prisma generate && bun run --hot src/index.ts",
    "start": "bunx prisma generate && bun src/index.ts",
    "test": "bun test src/test --preload ./src/test/setup.ts",
    "test:unit": "bun test src/test --preload ./src/test/setup.ts",
    "test:db": "bunx prisma generate && bun test prisma/integration/invariants.test.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3"
  },
  "prisma": {
    "seed": "bun run prisma/seed.ts"
  }
}
